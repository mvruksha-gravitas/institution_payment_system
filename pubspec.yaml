name: institutation_payment_system
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.6.0
dependencies:
  url_launcher: ^6.0.0
  image_picker: '>=1.1.2'
  firebase_storage: ^13.0.2
  fl_chart: 0.68.0
  intl: ^0.19.0
  printing: ^5.0.0
  pdf: ^3.0.0
  provider: any
  uuid: ^4.0.0
  shared_preferences: any
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_fonts: ^6.1.0
  firebase_core: ^4.1.1
  cloud_firestore: ^6.0.2
  firebase_auth: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
